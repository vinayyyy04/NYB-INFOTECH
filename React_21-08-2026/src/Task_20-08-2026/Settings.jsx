function Settings() {
  return (
    <div className="settings-card">
      <h2>⚙️ Settings</h2>

      <p>
        This component was loaded using Lazy Loading.
      </p>

      <button>Save Settings</button>
    </div>
  );
}

export default Settings;